# Next Actions

1. ~~Ledger settle on DELIVER~~ — done (`features/wallet`, shipments progress TX).
2. Run locally: `pnpm test`, `pnpm typecheck`, `pnpm --filter @yolla/web dev`.
3. Open PSP (iyzico/PayTR) + SMS accounts; wire webhook-signed `PAID`.
4. Rating model + post-delivery UI.
5. Admin incident queue + RETURN transition UX.
6. Land Playwright happy-path e2e.
