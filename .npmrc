# Metro (React Native bundler) pnpm'in sembolik link ağacını çözemiyor.
# Expo uygulamasının workspace paketlerini (@yolla/core) görebilmesi için
# düz (hoisted) node_modules gerekir — bkz. ADR 0003.
node-linker=hoisted
